# 后端装饰器

## NotLogin

有的接口可能不需要登录，就能够调用，比如登录接口。

这时候可以使用 `NotLogin` 装饰器，表示该接口不需要登录。

![alt text](image-36.png)

## NotAuth

有的接口可能不需要分配权限，只要登录进来就能够调用，比如获取用户信息接口。

这时候可以使用 `NotAuth` 装饰器，表示该接口不需要分配权限。

![alt text](image-37.png)
